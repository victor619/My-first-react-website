import React from "react";
import ReactDOM from "react-dom";

import Mywebsite from "./portal";

const rootElement = document.getElementById("root");
ReactDOM.render(
  <React.StrictMode>
    <Mywebsite />
  </React.StrictMode>,
  rootElement
);
